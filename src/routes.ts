export const privateRoutes = ["/where-to-stay", "/what-to-do", "/where-to-eat"];
export const authRoutes = ['/'];
export const DEFAULT_REDIRECT_LOGIN_URL = ['/'];
export const DEFAULT_REDIRECT_HOME_URL = ['/where-to-stay'];

